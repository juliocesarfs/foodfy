{% extends 'layout.njk' %}

{% block content %}
  <div class="list-recipes" id="recipes-list-recipes">

    {% for recipe in recipes %}
      <div class="recipe">
        <img src="{{ recipe.imageHome }}" class="recipe-image" >
        <h3>{{ recipe.title }}</h3>
        <p>{{ recipe.author }}</p>
      </div>
    {% endfor %}

  </div>

  <script src="scripts.js"></script>
{% endblock %}